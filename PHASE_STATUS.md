# Eventra build status

Phase 1 implementation has been started in the repository with an additive core-integrity migration and database session authorization service.

Phase 2-8 will extend the existing Eventra APIs and UI without replacing working functionality.

Production database migrations must be applied through the configured Neon production migration path before database-backed Phase 1 authentication is enabled in production.
