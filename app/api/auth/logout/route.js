import { COOKIE_NAME } from "../../../../lib/auth"; export async function POST(){const r=Response.json({ok:true});r.headers.set("Set-Cookie",`${COOKIE_NAME}=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0`);return r}