"use client";
import {useState} from "react";import {useRouter,useSearchParams} from "next/navigation";
export default function AdminLoginPage(){const router=useRouter(),params=useSearchParams();const[email,setEmail]=useState("owner@eventra.local"),[password,setPassword]=useState(""),[error,setError]=useState(""),[saving,setSaving]=useState(false);
async function submit(e){e.preventDefault();setSaving(true);setError("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to sign in");const next=params.get("next");router.replace(next&&next.startsWith("/dashboard")?next:"/dashboard");router.refresh()}catch(e){setError(e.message)}finally{setSaving(false)}}
return <main className="adminLoginPage"><div className="adminLoginGlow adminLoginGlowOne"/><div className="adminLoginGlow adminLoginGlowTwo"/><section className="adminLoginShell"><div className="adminLoginBrand"><a href="/" className="adminLoginLogo">eventra<span>.</span></a><span className="adminLoginBadge">CONTROL CENTER</span></div><div className="adminLoginCard"><div className="adminLoginIcon">e</div><div className="adminLoginEyebrow">ORGANISER ACCESS</div><h1>Welcome back.</h1><p className="adminLoginIntro">Sign in with your Eventra account.</p>{error&&<div className="adminLoginError">{error}</div>}<form onSubmit={submit}><label className="adminLoginField"><span>Email</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username" required/></label><label className="adminLoginField"><span>Password</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required autoFocus/></label><button className="adminLoginButton" type="submit" disabled={saving}>{saving?"Signing in…":"Enter Control Center"}{!saving&&<span>↗</span>}</button></form><div className="adminLoginFoot"><span><i/> Secure account access</span><a href="/">Back to website</a></div></div><p className="adminLoginCopyright">EVENTRA · EVENT MANAGEMENT</p></section></main>}