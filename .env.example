# Eventra admin access (first-admin bootstrap uses this only while users is empty)
# Set this in Vercel Project Settings → Environment Variables.
EVENTRA_ADMIN_PASSWORD=replace-with-a-strong-password
